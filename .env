# .env
SYMBOLS=XAUUSD           # comma-separated allowed symbols
TIMEFRAMES=M1,M5,M15,M30,H1,H4,D1        # any of: M1,M5,M15,M30,H1,H4,D1
BARS_PER_PULL=3000       # per timeframe per loop
TICKS_LOOKBACK_MIN=180   # last N minutes of ticks each loop
POLL_SECONDS=60          # loop interval
OUT_DIR=./data           # where CSVs will go
MAGIC=990015             # just a tag in logs
